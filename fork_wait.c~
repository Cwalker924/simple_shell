#include "header.h"

int main(int ac, char *av[], char *env)
{
	pid_t child = fork();

	if (child < 0)
	{
		perror("Error:");
		return (1);
	}
	if (child == 0)
	{
		printf("child");
		execve("/bin/ls", "ls", "/tmp");
	}
	else
	{
		wait(0);
	}
	return (0);
}
